# Temp / scratch
.tmp/
.out/
*.log

# Node
node_modules/

# Vendored Quartz checkout (managed by `npm run setup`, pinned by vendor/quartz-pin.json)
vendor/quartz/

# Build output
public/
.quartz-cache/
.build/

# Agent working notes (not deliverables)
IMPLEMENTATION__PRIVATE.md

# IDE
.idea/workspace.xml

# Canvas viewer bundle (built by `npm run bundle:viewer`)
canvas-plugin/dist/
